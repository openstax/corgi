production:
  session_secret: random(base64,128)
  postgres:
    server: "{{ postgres_server }}"
    db: "{{ postgres_db }}"
    user: "{{ postgres_user }}"
    password: "{{ postgres_password }}"
  github:
    api_token: ssm(/external/corgi/github/api_token)
    oauth_id: ssm(/external/corgi/github/oauth_id)
    oauth_secret: ssm(/external/corgi/github/oauth_secret)
