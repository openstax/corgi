version: '3.7'
services:
  backend:
    build:
      context: ./backend
      dockerfile: backend.dockerfile
      args:
        INSTALL_DEV: 'true'
  frontend:
    build:
      context: ./frontend
  docs:
    build:
      context: ./docs
